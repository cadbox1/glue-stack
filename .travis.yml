matrix:
  include:
    - language: java
      jdk: oraclejdk8
      services:
        - docker
      before_install:
        - cd api
      install: true
      script: mvn clean install
    - language: node_js
      node_js: "8"
      before_install:
        - cd ui
notifications:
  email: false
